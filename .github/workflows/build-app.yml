name: CI Caller

on:
  push:
    branches:
      - main

jobs:
  
  call-reusable:
    environment: local
    uses: ./.github/workflows/reusable.yml
    with:
      branch: ${{ github.ref_name }}
    secrets:
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}